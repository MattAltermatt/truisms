{
  "name": "truisms",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "gen": "node scripts/generate-json.mjs",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
